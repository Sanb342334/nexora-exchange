{
  "name": "p2p-exchange",
  "version": "1.0.0",
  "private": true,
  "description": "NEXORA crypto exchange — futures, P2P, Telegram Mini App",
  "scripts": {
    "build": "npm ci --prefix backend && npm run prisma:generate --prefix backend && npm run build --prefix backend",
    "start": "npm run start:prod --prefix backend",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "backend:install": "cd backend && npm install",
    "backend:dev": "cd backend && npm run start:dev",
    "backend:setup": "cd backend && npm run prisma:generate && npm run prisma:migrate && npm run db:seed",
    "frontend:install": "cd frontend && npm install",
    "frontend:dev": "cd frontend && npm run dev"
  }
}
