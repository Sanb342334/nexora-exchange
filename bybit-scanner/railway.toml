[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckTimeout = 300

# Worker service — HTTP port not required.
# Mount a Railway Volume on /app/logs to persist subscribers & logs.
