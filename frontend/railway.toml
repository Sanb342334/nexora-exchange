# Use when Railway service Root Directory = /frontend
# Set Config File absolute path: /frontend/railway.toml

[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
